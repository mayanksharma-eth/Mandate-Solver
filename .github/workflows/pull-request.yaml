name: mandate-solver-ci

on:
  pull_request:
  push:
    branches: [main]

jobs:
  solver:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - uses: Swatinem/rust-cache@v2
      - run: cargo fmt --package mandate-solver -- --check
      - run: cargo clippy --package mandate-solver --all-targets -- -D warnings
      - run: cargo test --package mandate-solver
      - run: cargo build --package mandate-solver --release
