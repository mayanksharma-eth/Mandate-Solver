name: deployment-disabled

on:
  workflow_dispatch:

jobs:
  deployment-disabled:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Automatic deployment is disabled during the Mandate Solver extraction."
          echo "A dedicated Mandate deployment workflow must be added and reviewed separately."
